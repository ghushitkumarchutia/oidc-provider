services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: oidc_provider
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
